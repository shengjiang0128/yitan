module.exports = {
  // 平时改 UI：enabled 保持 false，不会发网络请求，也不会 timeout
  baseUrl: "",
  enabled: false,

  // 联调时再改（二选一，整行复制，不要留空格）：
  // baseUrl: "http://127.0.0.1:3000",
  // baseUrl: "https://yitan-backend-8t63imq10-zbd299.vercel.app",
  // enabled: true
};
